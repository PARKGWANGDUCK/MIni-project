package miniProject.user;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

public class UserDAO {
	
	// DB로 회원 정보 옮기기
	private Connection connection;
	private PreparedStatement preparedStatement;
	private ResultSet result;
	
	
	public void insertUser(User user) {
		final String USER_INSERT = "INSERT into profile(?, ?, ?)";
//		final String USER_LIST = "SELECT * FROM profile ORDER BY user_id DESC";
//		final String USER_GET = "SELECT * FROM profile WHERE user_id = ?";
		
		
		try(Connection connection = DBIdPasswords.getConnection();
			PreparedStatement preparedStatement = connection.prepareStatement(USER_INSERT);
			preparedStatement.setInt(1, user.getUser_num());
			
			ResultSet resultSet = preparedStatement.executeQuery();
			
				) {
			while(resultSet.next()) {
				
			}
			} catch (Exception e) {
				e.printStackTrace();
			}
		
//		return false;
		
		}
		
//		PreparedStatement createPreparedStatement(Connection connection, String sql, int user_num, String user_id, String user_password) throws SQLException {
//			preparedStatement = connection.prepareStatement(sql);
//			preparedStatement.setInt(1,  user_num);			
//			return preparedStatement;
	
}
